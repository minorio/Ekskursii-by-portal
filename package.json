{
  "name": "ekskursii-by-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "preview": "preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass-embedded": "^1.100.0",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "flatpickr": "^4.6.13"
  }
}
